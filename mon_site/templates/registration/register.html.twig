{% include 'head.html' %}

{% block title %}{% endblock %}

{% include 'nav.html.twig' %}

{% block body %}

	{% block stylesheets %}
		<link rel="stylesheet" href="{{ asset('css/style.css') }}">
	{% endblock %}

	{% for flashError in app.flashes('verify_email_error') %}
		<div class="alert alert-danger" role="alert">{{ flashError }}</div>
	{% endfor %}

<div class="form-login-register">
	{{ form_start(registrationForm) }}
	<div class="container-fluid login-page-container py-4" id="login-form">
		<div class="row">
			<div class="col-xs-12 col-md-4 offset-md-4">
				<h1 class="h3 mb-3 font-weight-normal">S'inscrire</h1>

				{{ form_row(registrationForm.prenom, {
            label: 'Prénom:'
        }) }}
				{{ form_row(registrationForm.nom, {
            label: 'Nom:'
        }) }}
				{{ form_row(registrationForm.email, {
            label: 'Email:'
        }) }}
				{{ form_row(registrationForm.plainPassword, {
            label: 'Mot de passe:'
        }) }}
				{{ form_row(registrationForm.Cgu) }}

				<button type="submit" class="btn btn-sm btn-warning">S'inscrire</button>
			</div>
			<div></div>
			<div class="container-fluid login-page-container py-4">
				<div class="row">
					<div class="col-xs-12 col-md-4 offset-md-4">
						Vous avez déja un compte ? 🤔
						<a style="color: #000000" href="{{ path('app_login') }}">Connectez-vous !</a>

						{{ form_end(registrationForm) }}
						</div>
					{% endblock %}
