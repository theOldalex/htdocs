<form method="post" action="{{ path('users_delete', {'id': app.user.id}) }}" onsubmit="return confirm('Attention la suppression du compte est irréversible !');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ app.user.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer mon compte</button>
</form>
