{% extends 'users.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <h1>Edit User</h1>

    {{ include('users/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('users') }}">back to list</a>

    {{ include('users/_delete_form.html.twig') }}
{% endblock %}
